@page
@model FavAudioApp.Pages.Radios.CreateModel
@{
}

<h2 class="mb-3">New Radio</h2>
@if (!Model.errorMessage.Equals(""))
{
    <h2>@Model.errorMessage</h2>
}

<form method="POST">
    <div class="row mb-3">
        <label for="name" class="col-md-1 col-form-label">Name</label>
        <div>
            <input type="text" class="form-control" name="name"
                   id="name" placeholder="Enter name" value="@Model.RadioDto.Name" />
        </div>
    </div>
    <div class="row mb-3">
        <label for="url" class="col-md-1 col-form-label">URL</label>
        <div>
            <input type="url" class="form-control" name="url"
                   id="url" placeholder="Enter URL" value="@Model.RadioDto.Url" />
        </div>
    </div>
    <div class="row mb-3">
        <label for="genre" class="col-md-1 col-form-label">Genre</label>
        <div>
            <input type="text" class="form-control" name="genre"
                   id="genre" placeholder="Enter genre" value="@Model.RadioDto.Genre" />
        </div>
    </div>
    <div class="row mb-3">
        <label for="region" class="col-md-1 col-form-label">Region</label>
        <div>
            <input type="text" class="form-control" name="region"
                   id="region" placeholder="Enter region" value="@Model.RadioDto.Region" />
        </div>
    </div>
    <div class="row mb-3">
        <div class="offset-md-1 col-md-3 d-grid">
            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
        <div class="offset-md-1 col-md-3 d-grid">
            <a role="button" href="/Radios/Index" class="btn btn-outline-primary">Cancel</a>
        </div>
    </div>

</form>